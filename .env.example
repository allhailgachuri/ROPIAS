# Flask
FLASK_SECRET_KEY=generate-a-long-random-string-here
FLASK_ENV=development

# Database
# Development (SQLite — auto-created, no setup needed):
DATABASE_URL=sqlite:///database/ropias.db
# Production (PostgreSQL — provided by Railway/Render):
# DATABASE_URL=postgresql://user:password@host:5432/ropias

# Twilio WhatsApp
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

# Africa's Talking (SMS — for future use)
AT_USERNAME=your_username
AT_API_KEY=your_api_key
